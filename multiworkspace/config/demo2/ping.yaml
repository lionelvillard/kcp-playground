apiVersion: apps/v1
kind: Deployment
metadata:
  name: ping
  namespace: demo2-2
spec:
  selector:
    matchLabels:
      app: ping
  template:
    metadata:
      labels:
        app: ping
    spec:
      containers:
        - name: ping
          image: ko://playground/cmd/ping
          env:
            - name: TARGET
              value: "http://pong.demo2-1.svc.cluster.local"
